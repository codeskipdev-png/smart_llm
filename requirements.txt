# ---- SMART-LLM requirements (CUDA GPU machine) ----
# Install torch matching your CUDA toolkit first, e.g. for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt
#
# Pinned-ish lower bounds; newer patch versions are fine.

# --- core DL / LLM ---
torch>=2.2
transformers>=4.44          # Qwen2.5 support
accelerate>=0.33
peft>=0.12                  # UAAS (Contribution 2): LoRA adapters
safetensors>=0.4
sentencepiece>=0.2          # tokenizer backend for some backbones
einops>=0.7                 # some Qwen configs request it

# --- retrieval / embeddings ---
faiss-cpu>=1.8              # swap for faiss-gpu on the GPU box if desired
sentence-transformers>=3.0  # BAAI/bge-large-en-v1.5 / all-MiniLM

# --- explanation verification (Contribution 3) ---
captum>=0.7                 # Integrated Gradients

# --- data ---
datasets>=2.20             # AG News / TweetEval / Financial PhraseBank / PubMed
scikit-learn>=1.3          # 20 Newsgroups, metrics, calibration
pandas>=2.0
numpy>=1.24

# --- analysis / figures / paper ---
matplotlib>=3.7
scipy>=1.10
python-docx>=1.1           # manuscript + supplementary DOCX

# --- config / misc ---
PyYAML>=6.0
tqdm>=4.66
